namespace CardGame.Enums {

    public enum ActorType {
        ENEMY, PLAYER, ALLY
    }

    public enum RaceType {
        HUMAN, UNDEAD
    }

    public enum ClassType {
        FIGHTER, KNIGHT, TEMPLAR
    }
}